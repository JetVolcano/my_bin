[project]
name = "my-bin"
version = "1.0.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.7"
dependencies = []

[dependency-groups]
dev = [
    "jupyter>=1.1.1",
    "ruff==0.15.2",
]
